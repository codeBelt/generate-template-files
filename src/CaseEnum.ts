enum CaseEnum {
    None = '(noCase)',
    PascalCase = '(pascalCase)',
    CamelCase = '(camelCase)',
    KebabCase = '(kebabCase)',
    UpperCase = '(upperCase)',
    LowerCase = '(lowerCase)',
}

export default CaseEnum;
