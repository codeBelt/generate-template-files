import {~Model~ResponseModel} from './~Model~(kebabCase)-response.model';

export interface I~Component~ReducerState {
    readonly isLoading: boolean;
    readonly ~Model~(camelCase)Model: ~Model~ResponseModel;
}

~Component~(noCase)                 // What ever you entered in
~Component~(camelCase)         // downByTheRiver
~Component~(constantCase)   // DOWN_BY_THE_RIVER
~Component~(dotCase)             // down.by.the.river
~Component~(kebabCase)         // down-by-the-river
~Component~(lowerCase)         // downbytheriver
~Component~(pascalCase)       // DownByTheRiver
~Component~(pathCase)           // down/by/the/river
~Component~(sentenceCase)   // Down by the river
~Component~(snakeCase)         // down_by_the_river
~Component~(titleCase)         // Down By The River
