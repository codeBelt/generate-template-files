import {BaseModel} from 'sjs-base-model';

/*
    // Returned Api Data Sample
    {
    }
 */
export class ~Model~ResponseModel extends BaseModel {

    constructor(data: Partial<~Model~ResponseModel>) {
        super();

        this.update(data);
    }

    public update(data: Partial<~Model~ResponseModel>): void {
        super.update(data);
    }

}


~Model~(something)
